Ubuntu 14.04 Base Checklist:

1. Read the README and Answer Forensics Questions
	- use the internet if it's not an easy answer

420. run scripts m9 get on me level

2. sudo apt-get update & upgrade

3. Ensure secure sudo config
	- sudo cat -f /etc/sudoers.d/all (ALL ALL=NOPASSWD: ALL)

4. Change root password

5. Lock root account
	- sudo passwd -dl root

6. Software Center is installed

7. Delete any illegal games from games folder

8. Install cracklib
	- sudo apt-get install libpam-cracklib

9. Update third party applications

10. Disable username display
	- find /usr/ -iname "50-unity*"
	- sudo gedit
	- add "greeter-hide-users=true"
	- add "greeter-show-manual-login=true"

11. Disable guest account
	- open /usr/share/lightdm/lightdm.conf.d/50-Ubuntu.conf
	- add "allow-guest=false"
	- add "autologin-user=false"

12. Verify install
	- sudo nano /etc/pam.d/common-password
	- If there is the line "password prerequisite", then yay

13. Pam.d
	- cd /etc/pam.d
	- gedit common-password
	- change the line the setup script outputs to "password requisite 
		pam_cracklib.so try_first_pass retry=3 minlen=12 lcredit=-1 
		ucredit=-1 dcredit=-1 difok=3"

14. Empty file
	- sudo nano /etc/security/opasswd
	- check if it's empty
	- If not empty:
		- sudo rm /etc/security/opasswd
		- touch /etc/security/opasswd
		- chown root:root /etc/security/opasswd
		- sudo chmod 600 /etc/security/opasswd
		- ls -1
		- sudo nano /etc/pam.d/common-password
		- add "remember=24" at the end of "password [success=1 default=ignore] 
			pam_unix.so obscure use_authtok try_first_pass sha512"
	
15. Log and lock
	- sudo nano /etc/pam.d/common-auth
	- add "auth required pam_tally.so onerr=fail deny=3 unlocktime=900 
		audit even_deny_root_account silent" on highest 
		non commented line

16. Kill services
	- service status all
	- apt-get purge autoremove (service)

17. Software and updates
	- set auto check for updates to DAILY

18. Firewall
	- Install UFW:
		- sudo apt-get install ufw

	- Important File:
		- /etc/default/ufw

	- Status check:
		- sudo ufw status verbose

	- Enable/disable:
		- sudo ufw (enable/disable)

	- Default policy:
		- sudo ufw block incoming
		- sudo ufw allow outgoing

	- Rules:
		- sudo ufw (allow/block/deny) (service)

	- Port ranges:
		- sudo ufw (allow/block/deny) (port1):(port2)/(tcp/udp)

	- IP address based rules:
		- sudo ufw (allow/deny) from (ip)


19. Firefox Update
	- sudo add-apt-repository ppa:ubuntu-mozilla-security/ppa
	- sudo apt-get update
	- [sudo apt-get upgrade]

20. Refer to past competition tips

21. refer to past vulnerabilites

22. go through official checklist

23. check vulnerabilites from other images

24. update all the things